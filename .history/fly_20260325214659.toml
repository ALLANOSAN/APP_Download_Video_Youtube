app = "app-download-video-youtube"
primary_region = "gru"

[build]
  dockerfile = "Dockerfile"

[processes]
  app = "uvicorn src.server.main:app --host 0.0.0.0 --port 8000"

[http_service]
internal_port = 8000
force_https = true
auto_start_machines = true
min_machines_running = 1